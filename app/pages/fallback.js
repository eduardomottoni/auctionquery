// Fallback page to ensure there's at least one serverless page
export default function FallbackPage() {
  return (
    <div>
      <h1>Fallback Page</h1>
      <p>This is a fallback page to ensure serverless functions are built properly.</p>
    </div>
  );
} 